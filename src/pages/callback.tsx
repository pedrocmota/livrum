import React from 'react'

interface ICallback {
  //children: React.ReactNode
}

const Callback: React.FunctionComponent<ICallback> = () => {
  return (
    <div>
      ola
    </div>
  )
}

export default Callback